name: Release

on:
  push:
    tags: ["v*"]

permissions:
  artifact-metadata: write
  attestations: write
  contents: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - run: python3 scripts/build_release.py
      - name: Attest release files
        uses: actions/attest@v4
        with:
          subject-path: release/codefoldersync-*
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            release/codefoldersync-*.tar.gz
            release/codefoldersync-*.tar.gz.sha256
            release/codefoldersync-*.tar.gz.release.json
